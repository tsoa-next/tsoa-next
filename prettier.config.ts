import type { Config } from 'prettier'

const config: Config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  printWidth: 200,
  arrowParens: 'avoid',
}

module.exports = config
