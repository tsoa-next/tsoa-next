---
'@tsoa-next/cli': minor
'tsoa-next': minor
---

Add `tsoa generate` to auto-discover configs and update only changed route and OpenAPI outputs, plus `tsoa check` to detect stale generated outputs without writing files for CI enforcement.
